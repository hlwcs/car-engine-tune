<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Garage Tune Pro — Telegram Mini App</title>
    <script src="https://telegram.org/js/telegram-web-app.js"></script>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif;
        }

        :root {
            --bg-dark: #0a1a2b;
            --bg-card: rgba(18, 34, 58, 0.8);
            --accent: #ff7b00;
            --accent-glow: #ff9f4b;
            --text-primary: #ffffff;
            --text-secondary: #b8c7dc;
            --success: #2ecc71;
            --danger: #e74c3c;
        }

        body {
            background: linear-gradient(145deg, #0f2027 0%, #203a43 50%, #2c5364 100%);
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 12px;
            margin: 0;
            color: var(--text-primary);
        }

        /* Основной контейнер как в настоящем Telegram Mini App */
        .tg-mini-app {
            max-width: 420px;
            width: 100%;
            background: rgba(10, 25, 40, 0.75);
            backdrop-filter: blur(20px);
            -webkit-backdrop-filter: blur(20px);
            border-radius: 32px;
            padding: 20px 16px;
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.5), 0 0 0 1px rgba(255, 123, 0, 0.2);
            border: 1px solid rgba(255, 255, 255, 0.08);
            position: relative;
            overflow: hidden;
        }

        /* Анимированный фон */
        .tg-mini-app::before {
            content: '';
            position: absolute;
            top: -50%;
            left: -50%;
            width: 200%;
            height: 200%;
            background: radial-gradient(circle at 30% 50%, rgba(255, 123, 0, 0.08) 0%, transparent 50%);
            animation: rotate 30s linear infinite;
            z-index: -1;
        }

        @keyframes rotate {
            from { transform: rotate(0deg); }
            to { transform: rotate(360deg); }
        }

        /* Шапка приложения */
        .app-header {
            display: flex;
            align-items: center;
            justify-content: space-between;
            margin-bottom: 20px;
            padding: 0 4px;
        }

        .user-info {
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .user-avatar {
            width: 40px;
            height: 40px;
            background: linear-gradient(135deg, var(--accent), #ffbb00);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 20px;
            font-weight: bold;
            color: white;
            box-shadow: 0 4px 10px rgba(255, 123, 0, 0.3);
        }

        .user-name {
            font-weight: 600;
            font-size: 16px;
            color: white;
        }

        .user-status {
            font-size: 12px;
            color: var(--text-secondary);
            display: flex;
            align-items: center;
            gap: 4px;
        }

        .online-dot {
            width: 8px;
            height: 8px;
            background: var(--success);
            border-radius: 50%;
            display: inline-block;
        }

        /* Баланс и валюта */
        .balance-card {
            background: linear-gradient(145deg, #1e3b4f, #152a38);
            border-radius: 24px;
            padding: 18px 20px;
            margin-bottom: 24px;
            border: 1px solid rgba(255, 255, 255, 0.1);
            box-shadow: 0 10px 20px rgba(0, 0, 0, 0.3);
            position: relative;
            overflow: hidden;
        }

        .balance-card::after {
            content: '💰';
            position: absolute;
            right: 20px;
            top: 50%;
            transform: translateY(-50%);
            font-size: 48px;
            opacity: 0.1;
        }

        .balance-label {
            font-size: 14px;
            color: var(--text-secondary);
            margin-bottom: 6px;
            letter-spacing: 1px;
        }

        .balance-amount {
            font-size: 42px;
            font-weight: 800;
            color: var(--accent);
            text-shadow: 0 0 20px rgba(255, 123, 0, 0.5);
            line-height: 1;
            display: flex;
            align-items: baseline;
            gap: 5px;
        }

        .balance-currency {
            font-size: 20px;
            color: var(--text-secondary);
            font-weight: 400;
        }

        .balance-spent {
            font-size: 14px;
            color: var(--text-secondary);
            margin-top: 8px;
            display: flex;
            gap: 10px;
        }

        /* Гараж с машиной */
        .garage-view {
            background: #0d1c2c;
            border-radius: 28px;
            padding: 30px 20px;
            margin-bottom: 24px;
            border: 1px solid #2a4055;
            box-shadow: inset 0 -5px 0 #0a1520, 0 10px 20px rgba(0, 0, 0, 0.4);
            position: relative;
        }

        /* Машина (улучшенная CSS модель) */
        .car-container {
            position: relative;
            width: 100%;
            height: 140px;
            display: flex;
            flex-direction: column;
            align-items: center;
            filter: drop-shadow(0 10px 10px rgba(0, 0, 0, 0.5));
        }

        .car-body {
            width: 260px;
            height: 70px;
            background-color: #e74c3c;
            border-radius: 40px 40px 20px 20px;
            position: relative;
            box-shadow: 0 10px 0 #962d22;
            transition: all 0.3s cubic-bezier(0.175, 0.885, 0.32, 1.275);
            z-index: 2;
        }

        /* Стекло */
        .car-body::before {
            content: '';
            position: absolute;
            top: -18px;
            left: 50px;
            width: 90px;
            height: 30px;
            background: linear-gradient(145deg, #a8d8ff, #7bb3ff);
            border-radius: 30px 30px 10px 10px;
            border: 2px solid rgba(255, 255, 255, 0.3);
            box-shadow: 0 4px 0 #3d5a7a;
        }

        /* Фары */
        .car-body::after {
            content: '';
            position: absolute;
            top: 20px;
            right: -8px;
            width: 16px;
            height: 16px;
            background: #ffeeaa;
            border-radius: 50%;
            box-shadow: 0 0 15px #ffbb00, -2px 0 5px #ffaa00;
            border: 2px solid #ffdd99;
        }

        /* Задние фонари */
        .car-body .taillight {
            position: absolute;
            left: -8px;
            top: 20px;
            width: 16px;
            height: 16px;
            background: #ff5555;
            border-radius: 50%;
            box-shadow: 0 0 15px #ff0000;
            border: 2px solid #ff9999;
        }

        /* Колеса */
        .wheels {
            display: flex;
            justify-content: space-between;
            width: 200px;
            margin-top: -10px;
            position: relative;
            z-index: 3;
        }

        .wheel {
            width: 45px;
            height: 45px;
            background: radial-gradient(circle at 30% 30%, #444, #111);
            border-radius: 50%;
            border: 4px solid #777;
            box-shadow: 0 8px 0 #222, 0 0 0 2px #ffaa00;
            transition: all 0.3s ease;
            position: relative;
        }

        .wheel::after {
            content: '';
            display: block;
            width: 18px;
            height: 18px;
            background: #aaa;
            border-radius: 50%;
            margin: 10px auto;
            border: 3px solid #555;
            box-shadow: inset 0 0 5px #000;
        }

        .wheel-front {
            margin-left: 15px;
        }
        .wheel-rear {
            margin-right: 15px;
        }

        /* Тюнинг-стили - Обвес */
        .body-sport .car-body {
            border-bottom: 5px solid #ff9900;
            box-shadow: 0 10px 0 #962d22, 0 0 0 3px #ff9900;
        }

        .body-extreme .car-body {
            border-bottom: 10px double #ff5500;
            box-shadow: 0 10px 0 #962d22, 0 0 0 4px #ff3300;
            clip-path: polygon(0% 5%, 100% 0%, 95% 100%, 5% 95%);
        }

        /* Диски тюнинг */
        .wheels-alloy .wheel {
            background: radial-gradient(circle at 30% 30%, #e0e0e0, #a0a0a0);
            border: 4px solid #fff;
            box-shadow: 0 8px 0 #666, 0 0 0 3px #ffaa00;
        }
        .wheels-alloy .wheel::after {
            background: #ffdd44;
            border: 3px solid #bb9900;
        }

        .wheels-deep .wheel {
            background: #111;
            border: 5px solid #ffaa00;
            box-shadow: 0 8px 0 #b8860b, 0 0 0 3px #fff;
        }
        .wheels-deep .wheel::after {
            background: #ffaa00;
            border: 3px solid #fff;
        }

        /* Панели категорий */
        .section-title {
            display: flex;
            align-items: center;
            gap: 8px;
            margin-bottom: 16px;
        }

        .section-icon {
            width: 32px;
            height: 32px;
            background: var(--accent);
            border-radius: 12px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 18px;
        }

        .section-title h3 {
            font-size: 18px;
            font-weight: 600;
            color: white;
        }

        .tuning-grid {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 10px;
            margin-bottom: 24px;
        }

        .tuning-card {
            background: #1a2f40;
            border: 1px solid #2f4b62;
            border-radius: 20px;
            padding: 14px 5px;
            text-align: center;
            cursor: pointer;
            transition: all 0.2s ease;
            box-shadow: 0 5px 0 #0f1f2c;
            position: relative;
            overflow: hidden;
        }

        .tuning-card.active {
            background: linear-gradient(145deg, var(--accent), #ff9f1a);
            border-color: #ffc107;
            box-shadow: 0 5px 0 #b85e00, 0 0 15px #ff9f00;
            transform: translateY(-2px);
        }

        .tuning-card .part-name {
            font-size: 14px;
            font-weight: 600;
            margin-bottom: 4px;
        }

        .tuning-card .part-price {
            font-size: 16px;
            font-weight: 800;
            color: var(--accent);
        }

        .tuning-card.active .part-price {
            color: #fff;
        }

        .tuning-card .part-price::before {
            content: '$';
            font-size: 12px;
            opacity: 0.8;
            margin-right: 2px;
        }

        /* Цвета */
        .color-grid {
            display: grid;
            grid-template-columns: repeat(4, 1fr);
            gap: 10px;
            margin-bottom: 24px;
        }

        .color-option {
            height: 60px;
            border-radius: 20px;
            cursor: pointer;
            border: 3px solid transparent;
            transition: all 0.2s ease;
            box-shadow: 0 5px 0 rgba(0, 0, 0, 0.3);
            position: relative;
            overflow: hidden;
        }

        .color-option.active {
            border-color: white;
            box-shadow: 0 5px 0 #000, 0 0 20px #ffaa00;
            transform: scale(1.02);
        }

        .color-option.red { background: #e74c3c; }
        .color-option.blue { background: #3498db; }
        .color-option.black { background: #2c3e50; }
        .color-option.yellow { background: #f1c40f; }

        .color-option::after {
            content: '✓';
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            color: white;
            font-size: 24px;
            font-weight: bold;
            text-shadow: 0 2px 5px rgba(0,0,0,0.5);
            opacity: 0;
            transition: opacity 0.2s;
        }

        .color-option.active::after {
            opacity: 1;
        }

        /* Кнопки действий */
        .action-buttons {
            display: flex;
            gap: 12px;
            margin-top: 24px;
        }

        .btn-primary {
            flex: 2;
            background: linear-gradient(145deg, var(--accent), #ff9f1a);
            border: none;
            color: white;
            font-size: 18px;
            font-weight: 700;
            padding: 18px 12px;
            border-radius: 30px;
            cursor: pointer;
            box-shadow: 0 8px 0 #b85e00;
            transition: 0.1s ease;
            border: 1px solid #ffc107;
            text-transform: uppercase;
            letter-spacing: 1px;
        }

        .btn-primary:active {
            transform: translateY(4px);
            box-shadow: 0 4px 0 #b85e00;
        }

        .btn-secondary {
            flex: 1;
            background: #2c3e50;
            border: none;
            color: white;
            font-size: 18px;
            font-weight: 600;
            padding: 18px 5px;
            border-radius: 30px;
            cursor: pointer;
            box-shadow: 0 8px 0 #1a2632;
            transition: 0.1s ease;
            border: 1px solid #4a6a82;
        }

        .btn-secondary:active {
            transform: translateY(4px);
            box-shadow: 0 4px 0 #1a2632;
        }

        /* Уведомление */
        .toast {
            position: fixed;
            bottom: 30px;
            left: 50%;
            transform: translateX(-50%) translateY(100px);
            background: rgba(0, 0, 0, 0.9);
            backdrop-filter: blur(10px);
            color: white;
            padding: 14px 24px;
            border-radius: 40px;
            font-weight: 600;
            border: 1px solid var(--accent);
            box-shadow: 0 10px 30px rgba(0,0,0,0.5);
            transition: transform 0.3s cubic-bezier(0.68, -0.55, 0.265, 1.55);
            z-index: 1000;
            white-space: nowrap;
        }

        .toast.show {
            transform: translateX(-50%) translateY(0);
        }

        /* Анимация для машины */
        @keyframes bounce {
            0%, 100% { transform: translateY(0); }
            50% { transform: translateY(-5px); }
        }

        .car-container {
            animation: bounce 3s ease-in-out infinite;
        }
    </style>
</head>
<body>
    <div class="tg-mini-app" id="app">
        <!-- Шапка профиля -->
        <div class="app-header">
            <div class="user-info">
                <div class="user-avatar" id="userAvatar">👤</div>
                <div>
                    <div class="user-name" id="userName">Игрок</div>
                    <div class="user-status">
                        <span class="online-dot"></span>
                        <span id="userStatus">в гараже</span>
                    </div>
                </div>
            </div>
            <div>⚙️</div>
        </div>

        <!-- Баланс -->
        <div class="balance-card">
            <div class="balance-label">ТВОЙ БЮДЖЕТ</div>
            <div class="balance-amount">
                <span id="balanceAmount">5000</span>
                <span class="balance-currency">TUNER$</span>
            </div>
            <div class="balance-spent">
                <span>💸 Потрачено: <span id="spentAmount">0</span>$</span>
                <span>⭐ Осталось: <span id="remainingAmount">5000</span>$</span>
            </div>
        </div>

        <!-- Гараж с машиной -->
        <div class="garage-view">
            <div class="car-container" id="carModel">
                <div class="car-body" id="carBody">
                    <div class="taillight"></div>
                </div>
                <div class="wheels">
                    <div class="wheel wheel-front" id="wheelFront"></div>
                    <div class="wheel wheel-rear" id="wheelRear"></div>
                </div>
            </div>
        </div>

        <!-- Категория: Обвес -->
        <div class="section-title">
            <div class="section-icon">🏎️</div>
            <h3>Обвес кузова</h3>
        </div>
        <div class="tuning-grid" id="bodyKits">
            <div class="tuning-card active" data-part="body" data-value="stock" data-price="0">
                <div class="part-name">СТОК</div>
                <div class="part-price">0</div>
            </div>
            <div class="tuning-card" data-part="body" data-value="sport" data-price="500">
                <div class="part-name">СПОРТ</div>
                <div class="part-price">500</div>
            </div>
            <div class="tuning-card" data-part="body" data-value="extreme" data-price="1200">
                <div class="part-name">ЭКСТРИМ</div>
                <div class="part-price">1200</div>
            </div>
        </div>

        <!-- Категория: Диски -->
        <div class="section-title">
            <div class="section-icon">⚙️</div>
            <h3>Литые диски</h3>
        </div>
        <div class="tuning-grid" id="wheelsKits">
            <div class="tuning-card active" data-part="wheels" data-value="stock" data-price="0">
                <div class="part-name">СТОК</div>
                <div class="part-price">0</div>
            </div>
            <div class="tuning-card" data-part="wheels" data-value="alloy" data-price="300">
                <div class="part-name">СПЛАВ</div>
                <div class="part-price">300</div>
            </div>
            <div class="tuning-card" data-part="wheels" data-value="deep" data-price="600">
                <div class="part-name">DEEP</div>
                <div class="part-price">600</div>
            </div>
        </div>

        <!-- Категория: Цвет -->
        <div class="section-title">
            <div class="section-icon">🎨</div>
            <h3>Окраска</h3>
        </div>
        <div class="color-grid" id="colorOptions">
            <div class="color-option red active" data-color="red"></div>
            <div class="color-option blue" data-color="blue"></div>
            <div class="color-option black" data-color="black"></div>
            <div class="color-option yellow" data-color="yellow"></div>
        </div>

        <!-- Кнопки действий -->
        <div class="action-buttons">
            <button class="btn-secondary" id="resetBtn">🔄 Сброс</button>
            <button class="btn-primary" id="sendToBotBtn">ОТПРАВИТЬ</button>
        </div>
    </div>

    <!-- Всплывающее уведомление -->
    <div class="toast" id="toast">Готово!</div>

    <script>
        // Инициализация Telegram Web App
        const tg = window.Telegram.WebApp;
        tg.expand();
        tg.enableClosingConfirmation(); // Подтверждение при закрытии
        tg.setHeaderColor('#0a1a2b');
        tg.setBackgroundColor('#0f2027');

        // Конфигурация
        const BOT_TOKEN = '8229027130:AAEI8uuVc0jYWKArncUMEIdwdzxVTdSS_as';
        const TELEGRAM_API_URL = `https://api.telegram.org/bot${BOT_TOKEN}/sendMessage`;

        // Данные пользователя из Telegram
        const user = tg.initDataUnsafe?.user;
        if (user) {
            document.getElementById('userName').textContent = user.first_name || 'Игрок';
            document.getElementById('userAvatar').textContent = user.first_name ? user.first_name[0] : '👤';
        }

        // Состояние приложения
        const state = {
            body: 'stock',
            wheels: 'stock',
            color: 'red',
            budget: 5000,
            spent: 0
        };

        // Цены
        const prices = {
            body: { stock: 0, sport: 500, extreme: 1200 },
            wheels: { stock: 0, alloy: 300, deep: 600 }
        };

        // Элементы DOM
        const carModel = document.getElementById('carModel');
        const carBody = document.getElementById('carBody');
        const balanceEl = document.getElementById('balanceAmount');
        const spentEl = document.getElementById('spentAmount');
        const remainingEl = document.getElementById('remainingAmount');
        const toast = document.getElementById('toast');

        // Функция показа уведомления
        function showToast(message, duration = 2000) {
            toast.textContent = message;
            toast.classList.add('show');
            setTimeout(() => {
                toast.classList.remove('show');
            }, duration);
        }

        // Функция обновления цвета
        function updateColor(color) {
            const colors = {
                red: '#e74c3c',
                blue: '#3498db',
                black: '#2c3e50',
                yellow: '#f1c40f'
            };
            carBody.style.backgroundColor = colors[color] || '#e74c3c';
        }

        // Функция обновления классов машины
        function updateCarClasses() {
            carModel.className = 'car-container';
            carModel.classList.add(`body-${state.body}`);
            carModel.classList.add(`wheels-${state.wheels}`);
        }

        // Функция пересчета бюджета
        function recalcBudget() {
            const bodyPrice = prices.body[state.body] || 0;
            const wheelsPrice = prices.wheels[state.wheels] || 0;
            state.spent = bodyPrice + wheelsPrice;
            const remaining = 5000 - state.spent;

            balanceEl.textContent = remaining;
            spentEl.textContent = state.spent;
            remainingEl.textContent = remaining;

            // Подсветка если бюджет отрицательный
            if (remaining < 0) {
                balanceEl.style.color = '#ff6b6b';
                remainingEl.style.color = '#ff6b6b';
            } else {
                balanceEl.style.color = 'var(--accent)';
                remainingEl.style.color = 'var(--text-secondary)';
            }
        }

        // Функция отправки в Telegram
        async function sendToTelegram() {
            try {
                const chatId = user?.id;
                
                if (!chatId) {
                    showToast('❌ Ошибка: нет данных пользователя');
                    return;
                }

                const message = `🏁 <b>НОВЫЙ ТЮНИНГ!</b>\n\n` +
                    `👤 Игрок: ${user.first_name || 'Неизвестный'}\n` +
                    `🚗 Обвес: ${state.body.toUpperCase()}\n` +
                    `⚙️ Диски: ${state.wheels.toUpperCase()}\n` +
                    `🎨 Цвет: ${state.color.toUpperCase()}\n` +
                    `💰 Потрачено: ${state.spent}$\n` +
                    `💎 Остаток: ${5000 - state.spent}$\n\n` +
                    `#тюнинг #garage`;

                const response = await fetch(TELEGRAM_API_URL, {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify({
                        chat_id: chatId,
                        text: message,
                        parse_mode: 'HTML'
                    })
                });

                const data = await response.json();
                
                if (data.ok) {
                    showToast('✅ Отправлено в Telegram!');
                    tg.HapticFeedback.notificationOccurred('success'); // Вибрация
                } else {
                    showToast('❌ Ошибка отправки');
                }
            } catch (error) {
                console.error('Error:', error);
                showToast('❌ Ошибка соединения');
            }
        }

        // Обработчики для обвеса
        document.querySelectorAll('[data-part="body"]').forEach(card => {
            card.addEventListener('click', function() {
                // Убираем активный класс у всех карточек обвеса
                document.querySelectorAll('[data-part="body"]').forEach(c => c.classList.remove('active'));
                this.classList.add('active');
                
                // Обновляем состояние
                state.body = this.dataset.value;
                
                // Обновляем классы машины
                updateCarClasses();
                
                // Пересчитываем бюджет
                recalcBudget();
                
                // Тактильная отдача
                tg.HapticFeedback.impactOccurred('light');
            });
        });

        // Обработчики для дисков
        document.querySelectorAll('[data-part="wheels"]').forEach(card => {
            card.addEventListener('click', function() {
                document.querySelectorAll('[data-part="wheels"]').forEach(c => c.classList.remove('active'));
                this.classList.add('active');
                
                state.wheels = this.dataset.value;
                updateCarClasses();
                recalcBudget();
                
                tg.HapticFeedback.impactOccurred('light');
            });
        });

        // Обработчики для цвета
        document.querySelectorAll('.color-option').forEach(option => {
            option.addEventListener('click', function() {
                document.querySelectorAll('.color-option').forEach(o => o.classList.remove('active'));
                this.classList.add('active');
                
                state.color = this.dataset.color;
                updateColor(state.color);
                
                tg.HapticFeedback.impactOccurred('light');
            });
        });

        // Кнопка отправки
        document.getElementById('sendToBotBtn').addEventListener('click', sendToTelegram);

        // Кнопка сброса
        document.getElementById('resetBtn').addEventListener('click', function() {
            // Сброс состояния
            state.body = 'stock';
            state.wheels = 'stock';
            state.color = 'red';
            
            // Сброс UI
            document.querySelectorAll('[data-part="body"]').forEach(c => c.classList.remove('active'));
            document.querySelector('[data-part="body"][data-value="stock"]').classList.add('active');
            
            document.querySelectorAll('[data-part="wheels"]').forEach(c => c.classList.remove('active'));
            document.querySelector('[data-part="wheels"][data-value="stock"]').classList.add('active');
            
            document.querySelectorAll('.color-option').forEach(o => o.classList.remove('active'));
            document.querySelector('.color-option.red').classList.add('active');
            
            // Обновление машины
            updateCarClasses();
            updateColor('red');
            recalcBudget();
            
            tg.HapticFeedback.notificationOccurred('warning');
            showToast('🔄 Сброшено до стока');
        });

        // Инициализация
        updateCarClasses();
        updateColor('red');
        recalcBudget();

        // Настройка главной кнопки Telegram
        tg.MainButton.setText('🔥 СОХРАНИТЬ НАСТРОЙКИ');
        tg.MainButton.show();
        tg.MainButton.onClick(() => {
            sendToTelegram();
            tg.MainButton.hide();
        });

        // Отправляем данные боту при закрытии
        tg.onEvent('mainButtonClicked', function() {
            tg.sendData(JSON.stringify({
                action: 'save_config',
                user: user,
                config: state,
                spent: state.spent,
                remaining: 5000 - state.spent
            }));
        });

        console.log('Garage Tune Pro запущен!');
    </script>
</body>
</html>
